import Footer from "../../components/Footer/Footer";
import Header from "../../components/Header/Header";
import Head from "next/head";
import { useEffect, useState } from "react";
import HeroSection from "../../layouts/stories/HeroSection";
import Section2 from "../../layouts/stories/Section2";
import Section3 from "../../layouts/stories/Section3";
import Section4 from "../../layouts/stories/Section4";
import Section5 from "../../layouts/stories/Section5";

const Stories = () => {
  const [windowSize, setWindowSize] = useState({
    height: undefined,
  });

  useEffect(() => {
    function handleResize() {
      setWindowSize({
        height: window.scrollY,
      });
    }
    window.addEventListener("scroll", handleResize);
    handleResize();

    return () => window.removeEventListener("scroll", handleResize);
  }, []);

  return (
    <div>
      <Head>
        <title>PHOTONSNAP-Stories</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <Header />

      <main
        className={`w-full ${windowSize.height >= 200 ? "mt-[70px]" : "mt-0"}`}
      >
        <HeroSection />
        <Section2 />
        <Section3 />
        <Section4 />
        <Section5 />
      </main>

      <Footer />
    </div>
  );
};

export default Stories;
